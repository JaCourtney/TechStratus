# Dependencies (only if we ever add tooling)
node_modules/

# Cloudflare / Wrangler
.wrangler/
.dev.vars

# Internal brand/strategy docs — keep out of GitHub (may be a public repo)
TechStratus Brand & Marketing Overview.pdf
*.pdf

# OS / editor cruft
.DS_Store
Thumbs.db
*.log
.vscode/
.idea/
